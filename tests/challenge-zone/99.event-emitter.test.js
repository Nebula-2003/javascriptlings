import { describe, it, expect } from 'vitest';
import { EventEmitter } from '../../tasks/6.challenge-zone/99.event-emitter.js';

describe('EventEmitter', () => {
  it('should build an event emitter class', () => {
    // const emitter = new EventEmitter();
    // let count = 0;
    // emitter.on('test', () => { count++; });
    // emitter.emit('test');
    // expect(count).toEqual(1);
  });
});