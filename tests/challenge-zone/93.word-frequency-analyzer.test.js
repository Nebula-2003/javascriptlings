import { describe, it, expect } from 'vitest';
import { wordFrequencyAnalyzer } from '../../tasks/6.challenge-zone/93.word-frequency-analyzer.js';

describe('wordFrequencyAnalyzer', () => {
  it('should count and sort words by frequency', () => {
    // expect(wordFrequencyAnalyzer('hello world hello')).toEqual({hello: 2, world: 1});
  });
});