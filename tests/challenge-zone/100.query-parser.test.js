import { describe, it, expect } from 'vitest';
import { queryParser } from '../../tasks/6.challenge-zone/100.query-parser.js';

describe('queryParser', () => {
  it('should parse query string with nesting', () => {
    // expect(queryParser('a=1&b[c]=2')).toEqual({a: '1', b: {c: '2'}});
  });
});