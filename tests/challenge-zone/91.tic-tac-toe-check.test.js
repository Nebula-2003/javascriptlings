import { describe, it, expect } from 'vitest';
import { ticTacToeCheck } from '../../tasks/6.challenge-zone/91.tic-tac-toe-check.js';

describe('ticTacToeCheck', () => {
  it('should check winner in tic-tac-toe grid', () => {
    // expect(ticTacToeCheck([['X', 'O', 'X'], ['O', 'X', 'O'], ['X', 'O', 'X']])).toEqual('X');
  });
});