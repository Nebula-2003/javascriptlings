import { describe, it, expect } from 'vitest';
import { minStepsToZero } from '../../tasks/6.challenge-zone/92.min-steps-to-zero.js';

describe('minStepsToZero', () => {
  it('should return min operations to reach 0', () => {
    // expect(minStepsToZero(5)).toEqual(4); // 5 -> 4 -> 2 -> 1 -> 0
  });
});