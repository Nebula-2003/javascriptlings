import { describe, it, expect } from 'vitest';
import { urlShortenerAlgo } from '../../tasks/6.challenge-zone/97.url-shortener-algo.js';

describe('urlShortenerAlgo', () => {
  it('should encode & decode short URLs', () => {
    // const shortUrl = urlShortenerAlgo('https://www.google.com');
    // expect(urlShortenerAlgo(shortUrl)).toEqual('https://www.google.com');
  });
});