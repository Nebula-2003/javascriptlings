import { describe, it, expect } from 'vitest';
import { deepEqualArrays } from '../../tasks/6.challenge-zone/96.deep-equal-arrays.js';

describe('deepEqualArrays', () => {
  it('should deep compare arrays of objects', () => {
    // expect(deepEqualArrays([{a: 1}], [{a: 1}])).toEqual(true);
  });
});