import { describe, it, expect } from 'vitest';
import { diffObjects } from '../../tasks/6.challenge-zone/98.diff-objects.js';

describe('diffObjects', () => {
  it('should return recursive object diff tool', () => {
    // expect(diffObjects({a: 1, b: {c: 2}}, {a: 1, b: {c: 3}})).toEqual({b: {c: 3}});
  });
});