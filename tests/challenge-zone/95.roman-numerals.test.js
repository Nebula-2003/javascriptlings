import { describe, it, expect } from 'vitest';
import { romanNumerals } from '../../tasks/6.challenge-zone/95.roman-numerals.js';

describe('romanNumerals', () => {
  it('should convert number to Roman numeral', () => {
    // expect(romanNumerals(9)).toEqual('IX');
  });
});