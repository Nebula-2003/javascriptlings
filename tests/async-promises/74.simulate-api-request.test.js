import { describe, it, expect } from 'vitest';
import { simulateApiRequest } from '../../tasks/4.async-promises/74.simulate-api-request.js';

describe('simulateApiRequest', () => {
  it('should build mock API delay function', async () => {
    // const result = await simulateApiRequest({data: 'test'}, 100);
    // expect(result).toEqual({data: 'test'});
  });
});