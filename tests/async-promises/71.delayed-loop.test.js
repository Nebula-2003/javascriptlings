import { describe, it, expect } from 'vitest';
import { delayedLoop } from '../../tasks/4.async-promises/71.delayed-loop.js';

describe('delayedLoop', () => {
  it('should print with delay in loop', async () => {
    // const logs = [];
    // await delayedLoop([1, 2, 3], 50, (item) => logs.push(item));
    // expect(logs).toEqual([1, 2, 3]);
  });
});