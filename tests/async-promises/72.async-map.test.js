import { describe, it, expect } from 'vitest';
import { asyncMap } from '../../tasks/4.async-promises/72.async-map.js';

describe('asyncMap', () => {
  it('should map values with async transformation', async () => {
    // const result = await asyncMap([1, 2, 3], async (num) => num * 2);
    // expect(result).toEqual([2, 4, 6]);
  });
});