import { describe, it, expect } from 'vitest';
import { progressBar } from '../../tasks/4.async-promises/75.progress-bar.js';

describe('progressBar', () => {
  it('should simulate a timed progress update', async () => {
    // let progress = 0;
    // await progressBar(100, (p) => { progress = p; });
    // expect(progress).toEqual(100);
  });
});