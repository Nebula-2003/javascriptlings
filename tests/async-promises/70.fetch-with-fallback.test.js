import { describe, it, expect } from 'vitest';
import { fetchWithFallback } from '../../tasks/4.async-promises/70.fetch-with-fallback.js';

describe('fetchWithFallback', () => {
  it('should try first URL, fallback to second', async () => {
    // const result = await fetchWithFallback(['invalid-url', 'https://jsonplaceholder.typicode.com/todos/1']);
    // expect(result).toHaveProperty('id', 1);
  });
});