import { describe, it, expect } from 'vitest';
import { partial } from '../../tasks/5.higher-order-functional-concepts/84.partial.js';

describe('partial', () => {
  it('should create partial function', () => {
    // const add = (a, b) => a + b;
    // const add5 = partial(add, 5);
    // expect(add5(10)).toEqual(15);
  });
});