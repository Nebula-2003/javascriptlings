import { describe, it, expect } from 'vitest';
import { dynamicSort } from '../../tasks/5.higher-order-functional-concepts/89.dynamic-sort.js';

describe('dynamicSort', () => {
  it('should sort by dynamic property', () => {
    // const arr = [{name: 'b'}, {name: 'a'}];
    // expect(arr.sort(dynamicSort('name'))).toEqual([{name: 'a'}, {name: 'b'}]);
  });
});