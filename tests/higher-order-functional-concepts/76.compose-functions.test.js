import { describe, it, expect } from 'vitest';
import { composeFunctions } from '../../tasks/5.higher-order-functional-concepts/76.compose-functions.js';

describe('composeFunctions', () => {
  it('should compose two functions', () => {
    // const add1 = (a) => a + 1;
    // const mul2 = (a) => a * 2;
    // const composed = composeFunctions(mul2, add1);
    // expect(composed(5)).toEqual(12); // (5 + 1) * 2
  });
});