import { describe, it, expect } from 'vitest';
import { arrayToTree } from '../../tasks/5.higher-order-functional-concepts/87.array-to-tree.js';

describe('arrayToTree', () => {
  it('should convert flat list to tree', () => {
    // const arr = [{id: 1, parentId: null}, {id: 2, parentId: 1}];
    // const tree = arrayToTree(arr);
    // expect(tree).toEqual([{id: 1, parentId: null, children: [{id: 2, parentId: 1, children: []}]}]);
  });
});