import { describe, it, expect } from 'vitest';
import { debounce } from '../../tasks/5.higher-order-functional-concepts/78.debounce.js';

describe('debounce', () => {
  it('should implement debounce wrapper', async () => {
    // let count = 0;
    // const increment = debounce(() => { count++; }, 100);
    // increment();
    // increment();
    // increment();
    // await new Promise(resolve => setTimeout(resolve, 150));
    // expect(count).toEqual(1);
  });
});