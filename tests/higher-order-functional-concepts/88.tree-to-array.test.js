import { describe, it, expect } from 'vitest';
import { treeToArray } from '../../tasks/5.higher-order-functional-concepts/88.tree-to-array.js';

describe('treeToArray', () => {
  it('should flatten tree to list', () => {
    // const tree = [{id: 1, parentId: null, children: [{id: 2, parentId: 1, children: []}]}];
    // const arr = treeToArray(tree);
    // expect(arr).toEqual([{id: 1, parentId: null}, {id: 2, parentId: 1}]);
  });
});