import { describe, it, expect } from 'vitest';
import { once } from '../../tasks/5.higher-order-functional-concepts/82.once.js';

describe('once', () => {
  it('should ensure function runs once only', () => {
    // let count = 0;
    // const increment = once(() => { count++; });
    // increment();
    // increment();
    // expect(count).toEqual(1);
  });
});