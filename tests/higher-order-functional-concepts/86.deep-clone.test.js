import { describe, it, expect } from 'vitest';
import { deepClone } from '../../tasks/5.higher-order-functional-concepts/86.deep-clone.js';

describe('deepClone', () => {
  it('should deep clone an object or array', () => {
    // const obj = {a: 1, b: {c: 2}};
    // const cloned = deepClone(obj);
    // expect(cloned).toEqual(obj);
    // expect(cloned).not.toBe(obj);
  });
});