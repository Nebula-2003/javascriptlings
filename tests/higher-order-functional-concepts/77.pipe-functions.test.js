import { describe, it, expect } from 'vitest';
import { pipeFunctions } from '../../tasks/5.higher-order-functional-concepts/77.pipe-functions.js';

describe('pipeFunctions', () => {
  it('should pipe functions left to right', () => {
    // const add1 = (a) => a + 1;
    // const mul2 = (a) => a * 2;
    // const piped = pipeFunctions(add1, mul2);
    // expect(piped(5)).toEqual(12); // (5 + 1) * 2
  });
});