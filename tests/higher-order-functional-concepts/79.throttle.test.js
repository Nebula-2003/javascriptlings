import { describe, it, expect } from 'vitest';
import { throttle } from '../../tasks/5.higher-order-functional-concepts/79.throttle.js';

describe('throttle', () => {
  it('should implement throttle wrapper', async () => {
    // let count = 0;
    // const increment = throttle(() => { count++; }, 100);
    // increment();
    // increment();
    // increment();
    // await new Promise(resolve => setTimeout(resolve, 150));
    // expect(count).toEqual(1);
  });
});