import { describe, it, expect } from 'vitest';
import { functionLogger } from '../../tasks/5.higher-order-functional-concepts/81.function-logger.js';

describe('functionLogger', () => {
  it('should log input/output of function', () => {
    // const add = (a, b) => a + b;
    // const loggedAdd = functionLogger(add);
    // expect(loggedAdd(1, 2)).toEqual(3);
  });
});