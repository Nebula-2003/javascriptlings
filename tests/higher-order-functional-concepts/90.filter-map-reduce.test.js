import { describe, it, expect } from 'vitest';
import { filterMapReduce } from '../../tasks/5.higher-order-functional-concepts/90.filter-map-reduce.js';

describe('filterMapReduce', () => {
  it('should chain all three together', () => {
    // const arr = [1, 2, 3, 4, 5];
    // const result = filterMapReduce(arr, (n) => n % 2 === 0, (n) => n * 2, (acc, n) => acc + n, 0);
    // expect(result).toEqual(12); // (2*2) + (4*2) = 4 + 8 = 12
  });
});