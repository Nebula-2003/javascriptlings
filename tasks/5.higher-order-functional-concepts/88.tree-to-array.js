/**
 * Converts a tree structure to a flat array of objects.
 * @param {object} tree The root node of the tree structure.
 * @returns {object[]} The flat array of objects.
 */
export function treeToArray(tree) {

}
