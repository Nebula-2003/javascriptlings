/**
 * Creates a function that retries the provided function a specified number of times.
 * @param {Function} fn The function to retry.
 * @param {number} retries The number of times to retry.
 * @returns {Function} The new retrying function.
 */
export function retryWrapper(fn, retries) {

}
