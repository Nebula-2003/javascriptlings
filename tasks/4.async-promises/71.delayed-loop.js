/**
 * Loops through an array with a delay between each iteration.
 * @param {any[]} arr The array to loop through.
 * @param {number} delay The delay in milliseconds between iterations.
 * @returns {Promise<void>} A promise that resolves when the loop is complete.
 * @example delayedLoop([1, 2, 3], 100) // Logs 1, 2, 3 with 100ms delay between each
 */
export function delayedLoop(arr, delay) {

}
