/**
 * Simulates an API request that resolves with given data after a delay.
 * @param {any} data The data to resolve with.
 * @param {number} delay The delay in milliseconds.
 * @returns {Promise<any>} A promise that resolves with the data after the delay.
 * @example simulateApiRequest({ user: 'John' }, 1000) // Resolves with { user: 'John' } after 1 second
 */
export function simulateApiRequest(data, delay) {

}
