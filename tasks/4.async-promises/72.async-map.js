/**
 * Maps an array to a new array using an asynchronous mapping function.
 * @param {any[]} arr The array to map.
 * @param {Function} asyncFn An asynchronous function to apply to each element.
 * @returns {Promise<any[]>} A promise that resolves with the new mapped array.
 * @example asyncMap([1, 2, 3], async (n) => n * 2) // Resolves with [2, 4, 6]
 */
export function asyncMap(arr, asyncFn) {

}
