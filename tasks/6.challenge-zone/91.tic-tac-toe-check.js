/**
 * Checks the state of a Tic-Tac-Toe board.
 * @param {string[][]} board The Tic-Tac-Toe board.
 * @returns {string} 'X' if X has won, 'O' if O has won, 'draw' if it's a draw, and 'in progress' if the game is not over.
 */
export function ticTacToeCheck(board) {

}
