/**
 * Finds all prime numbers up to a given number n.
 * @param {number} n The upper limit to find prime numbers.
 * @returns {number[]} An array of prime numbers.
 * @example findPrimes(10) // [2, 3, 5, 7]
 */
export function findPrimes(n) {

}