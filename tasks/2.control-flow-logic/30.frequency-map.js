/**
 * Creates a frequency map of elements in an array.
 * The map should store elements as keys and their frequencies as values.
 * @param {any[]} arr The array to create a frequency map from.
 * @returns {Object.<string, number>} A frequency map of the elements.
 * @example frequencyMap(['a', 'b', 'a', 'c', 'b', 'a']) // { a: 3, b: 2, c: 1 }
 */
export function frequencyMap(arr) {

}