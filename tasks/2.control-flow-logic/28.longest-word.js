/**
 * Finds the longest word in a sentence.
 * If there are multiple words with the same maximum length, return the first one.
 * @param {string} str The sentence to search.
 * @returns {string} The longest word in the sentence.
 * @example longestWord("The quick brown fox jumped over the lazy dog") // "jumped"
 */
export function longestWord(str) {

}