/**
 * Squares each number in an array.
 * @param {number[]} arr The input array of numbers.
 * @returns {number[]} A new array with each number squared.
 * @example squareNumbers([1, 2, 3]) // [1, 4, 9]
 */
export function squareNumbers(arr) {

}
