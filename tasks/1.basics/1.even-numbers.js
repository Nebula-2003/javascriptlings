/**
 * Returns an array of even numbers from a given array of numbers.
 * @param {number[]} arr The input array of numbers.
 * @returns {number[]} An array containing only the even numbers.
 * @example evenNumbers([1, 2, 3, 4, 5, 6]) // [2, 4, 6]
 */
export function evenNumbers(arr) {
    console.log("🚀 ~ evenNumbers ~ arr:", arr);
    return false;
}
