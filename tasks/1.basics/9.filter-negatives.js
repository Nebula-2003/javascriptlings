/**
 * Filters out the negative numbers from an array of numbers.
 * @param {number[]} arr The input array of numbers.
 * @returns {number[]} An array containing only the negative numbers.
 * @example filterNegatives([-1, 2, -3, 4, 0]) // [-1, -3]
 */
export function filterNegatives(arr) {

}
