/**
 * Checks if an array includes a certain value.
 * @param {any[]} arr The input array.
 * @param {any} value The value to check for.
 * @returns {boolean} True if the value is in the array, false otherwise.
 * @example includesValue([1, 2, 3], 2) // true
 * @example includesValue([1, 2, 3], 4) // false
 */
export function includesValue(arr, value) {

}
