/**
 * Finds the maximum number in an array of numbers.
 * @param {number[]} arr The input array of numbers.
 * @returns {number} The maximum number in the array.
 * @example maxNumber([1, 5, 2, 8, 3]) // 8
 */
export function maxNumber(arr) {

}
