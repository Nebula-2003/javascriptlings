/**
 * Returns an array of odd numbers from a given array of numbers.
 * @param {number[]} arr The input array of numbers.
 * @returns {number[]} An array containing only the odd numbers.
 * @example oddNumbers([1, 2, 3, 4, 5, 6]) // [1, 3, 5]
 */
export function oddNumbers(arr) {

}
