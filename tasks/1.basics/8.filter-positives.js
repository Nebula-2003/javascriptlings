/**
 * Filters out the positive numbers from an array of numbers.
 * @param {number[]} arr The input array of numbers.
 * @returns {number[]} An array containing only the positive numbers.
 * @example filterPositives([-1, 2, -3, 4, 0]) // [2, 4]
 */
export function filterPositives(arr) {

}
