/**
 * Finds the minimum number in an array of numbers.
 * @param {number[]} arr The input array of numbers.
 * @returns {number} The minimum number in the array.
 * @example minNumber([1, 5, 2, 8, 3]) // 1
 */
export function minNumber(arr) {

}
