/**
 * Checks if any number in an array is negative.
 * @param {number[]} arr The input array of numbers.
 * @returns {boolean} True if any number is negative, false otherwise.
 * @example anyNegative([1, -2, 3]) // true
 * @example anyNegative([1, 2, 3]) // false
 */
export function anyNegative(arr) {

}
