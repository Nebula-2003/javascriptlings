/**
 * Maps the values of an object to a new object using a callback function.
 * @param {object} obj The input object.
 * @param {Function} fn The callback function to apply to each value.
 * @returns {object} A new object with the mapped values.
 * @example objectMapValues({ a: 1, b: 2 }, (n) => n * 2) // { a: 2, b: 4 }
 */
export function objectMapValues(obj, fn) {

}
