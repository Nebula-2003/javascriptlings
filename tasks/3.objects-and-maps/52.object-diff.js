/**
 * Finds the difference between two objects.
 * @param {object} obj1 The first object.
 * @param {object} obj2 The second object.
 * @returns {object} An object containing the key-value pairs that are different.
 * @example objectDiff({ a: 1, b: 2 }, { a: 1, c: 3 }) // { b: 2, c: 3 }
 */
export function objectDiff(obj1, obj2) {

}
