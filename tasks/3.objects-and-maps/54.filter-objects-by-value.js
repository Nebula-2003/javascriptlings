/**
 * Filters an array of objects based on a key-value pair.
 * @param {object[]} arr The input array of objects.
 * @param {string} key The key to filter by.
 * @param {any} value The value to match.
 * @returns {object[]} An array of objects that match the key-value pair.
 * @example filterObjectsByValue([{ a: 1, b: 2 }, { a: 1, b: 3 }], 'a', 1) // [{ a: 1, b: 2 }, { a: 1, b: 3 }]
 */
export function filterObjectsByValue(arr, key, value) {

}
