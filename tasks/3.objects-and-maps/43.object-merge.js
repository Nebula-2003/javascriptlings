/**
 * Merges two objects into a new object.
 * @param {object} obj1 The first object.
 * @param {object} obj2 The second object.
 * @returns {object} A new object containing properties from both objects.
 * @example objectMerge({ a: 1 }, { b: 2 }) // { a: 1, b: 2 }
 */
export function objectMerge(obj1, obj2) {

}
