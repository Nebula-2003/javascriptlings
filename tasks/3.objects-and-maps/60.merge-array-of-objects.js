/**
 * Merges an array of objects into a single object.
 * @param {object[]} arr The input array of objects.
 * @returns {object} A new object containing all properties from the objects in the array.
 * @example mergeArrayOfObjects([{ a: 1 }, { b: 2 }, { c: 3 }]) // { a: 1, b: 2, c: 3 }
 */
export function mergeArrayOfObjects(arr) {

}
