/**
 * Converts a URL query string to an object.
 * @param {string} str The URL query string.
 * @returns {object} The resulting object.
 * @example queryStringToObject("a=1&b=hello") // { a: '1', b: 'hello' }
 */
export function queryStringToObject(str) {

}
