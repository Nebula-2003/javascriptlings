/**
 * Creates an object from an array of key-value pairs.
 * @param {any[][]} arr The input array of pairs.
 * @returns {object} The resulting object.
 * @example objectFromPairs([['a', 1], ['b', 2]]) // { a: 1, b: 2 }
 */
export function objectFromPairs(arr) {

}
