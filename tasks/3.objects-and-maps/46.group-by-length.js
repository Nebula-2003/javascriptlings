/**
 * Groups an array of strings by their length.
 * @param {string[]} arr The input array of strings.
 * @returns {object} An object where keys are string lengths and values are arrays of strings.
 * @example groupByLength(['one', 'two', 'three', 'four']) // { '3': ['one', 'two'], '5': ['three'], '4': ['four'] }
 */
export function groupByLength(arr) {

}
