/**
 * Counts the occurrences of objects in an array, grouped by a specified property.
 * @param {object[]} arr The input array of objects.
 * @param {string} prop The property to group by.
 * @returns {object} An object where keys are property values and values are the counts.
 * @example countByProperty([{ type: 'A' }, { type: 'B' }, { type: 'A' }], 'type') // { 'A': 2, 'B': 1 }
 */
export function countByProperty(arr, prop) {

}
