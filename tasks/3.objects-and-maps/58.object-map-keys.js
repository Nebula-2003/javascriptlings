/**
 * Maps the keys of an object to a new object using a callback function.
 * @param {object} obj The input object.
 * @param {Function} fn The callback function to apply to each key.
 * @returns {object} A new object with the mapped keys.
 * @example objectMapKeys({ a: 1, b: 2 }, (k) => k.toUpperCase()) // { A: 1, B: 2 }
 */
export function objectMapKeys(obj, fn) {

}
