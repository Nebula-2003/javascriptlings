/**
 * Creates a function that invokes the provided function with partially applied arguments.
 * @param {Function} fn The function to partially apply arguments to.
 * @param {...any} args The arguments to partially apply.
 * @returns {Function} The new partially applied function.
 */
export function partial(fn, ...args) {

}
