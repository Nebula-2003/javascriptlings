/**
 * Converts a flat array of objects to a tree structure.
 * @param {object[]} arr The flat array of objects.
 * @returns {object} The root node of the tree structure.
 */
export function arrayToTree(arr) {

}
